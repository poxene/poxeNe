```ts
console.log('hello, world!');

const workingWith = ['node', 'react', 'next', 'typescript', 'tailwind', 'postgres', 'supabase'];
const playingWith = ['vite', 'express', 'python', 'gdscript', 'drizzle', 'prisma'];

console.log('learning: ', [...workingWith, ...playingWith].join());
```
